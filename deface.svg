<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 800 600" preserveAspectRatio="xMidYMid slice">
  <rect width="100%" height="100%" fill="#000" />
  
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        @import url('https://fonts.googleapis.com/css2?family=Courier+Prime:wght@700&amp;display=swap');

        body {
          background: #000;
          color: #f00;
          font-family: 'Courier Prime', monospace;
          margin: 0;
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
          overflow: hidden;
          text-align: center;
        }

        .glitch-container {
          position: relative;
        }

        /* Efek Glitch Utama */
        .main-title {
          font-size: 80px;
          position: relative;
          color: white;
          letter-spacing: 10px;
          animation: glitch 1s linear infinite;
        }

        .main-title::before, .main-title::after {
          content: "./KSOTARIA";
          position: absolute;
          top: 0; left: 0; width: 100%; height: 100%;
          background: black;
        }

        .main-title::before {
          left: 2px;
          text-shadow: -2px 0 #ff00c1;
          clip: rect(44px, 450px, 56px, 0);
          animation: glitch-anim 5s infinite linear alternate-reverse;
        }

        .main-title::after {
          left: -2px;
          text-shadow: -2px 0 #00fff9, 2px 2px #ff00c1;
          animation: glitch-anim2 1s infinite linear alternate-reverse;
        }

        .info-box {
          border: 1px solid #f00;
          padding: 20px;
          margin-top: 20px;
          background: rgba(255, 0, 0, 0.1);
          box-shadow: 0 0 20px #f00;
          animation: blink 0.1s infinite;
        }

        .status { color: #fff; text-shadow: 0 0 5px #f00; }

        @keyframes glitch-anim {
          0% { clip: rect(31px, 9999px, 94px, 0); }
          20% { clip: rect(62px, 9999px, 42px, 0); }
          40% { clip: rect(15px, 9999px, 61px, 0); }
          60% { clip: rect(80px, 9999px, 5px, 0); }
          80% { clip: rect(44px, 9999px, 88px, 0); }
          100% { clip: rect(12px, 9999px, 31px, 0); }
        }

        @keyframes blink {
          0% { opacity: 1; }
          50% { opacity: 0.8; }
          100% { opacity: 1; }
        }

        .scanline {
          width: 100%; height: 100px;
          z-index: 10;
          background: linear-gradient(0deg, rgba(0, 0, 0, 0) 0%, rgba(255, 0, 0, 0.2) 50%, rgba(0, 0, 0, 0) 100%);
          opacity: 0.1;
          position: absolute;
          bottom: 100%;
          animation: scanline 3s linear infinite;
        }

        @keyframes scanline {
          0% { bottom: 100%; }
          100% { bottom: -100%; }
        }
      </style>

      <div class="scanline"></div>
      <div class="glitch-container">
        <h1 class="main-title">./KSOTARIA</h1>
        <div class="info-box">
          <p class="status">STATUS: SYSTEM COMPROMISED BY 1337</p>
          <p>[ GREETZ: ALL INDONESIAN HACKER ]</p>
          <p style="color: #555;">#FuckYou_Mafia</p>
        </div>
      </div>
    </div>
  </foreignObject>

  <script type="text/javascript">
    <![CDATA[
      // XSS Trigger
      try {
        alert("hacked by ksotaria1337");
        window.onload = function() {
          document.title = "HACKED BY KSOTARIA";
          console.log("%c SECURITY BREACHED ", "background: red; color: white; font-size: 30px;");
        };
      } catch(e) {}
    ]]>
  </script>
</svg>
